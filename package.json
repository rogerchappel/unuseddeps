{
  "name": "unuseddeps",
  "version": "0.1.0",
  "description": "Detect unused Node.js dependencies — zero config, local-first, CI-friendly",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "unuseddeps": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "check": "tsc --noEmit && npm run lint",
    "demo": "bash demo/run-fixture-scan.sh",
    "smoke": "tsx src/cli.ts fixtures/all-clean",
    "dependencies:check": "tsx src/cli.ts . --no-include-dev",
    "audit:all": "npm audit",
    "audit:prod": "npm audit --omit=dev",
    "package:smoke": "npm run build && node scripts/package-smoke.mjs",
    "release:check": "npm run audit:all && npm run audit:prod && npm run check && npm test && npm run dependencies:check && npm run build && npm run smoke && npm run package:smoke",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/unuseddeps.git"
  },
  "homepage": "https://github.com/rogerchappel/unuseddeps#readme",
  "bugs": {
    "url": "https://github.com/rogerchappel/unuseddeps/issues"
  },
  "keywords": [
    "dependencies",
    "unused",
    "nodejs",
    "typescript",
    "cli",
    "ci",
    "package.json",
    "cleanup",
    "audit"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "glob": "^13.0.6",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vitest/coverage-v8": "^3.2.7",
    "eslint": "^9.6.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.59.4",
    "vitest": "^3.2.7"
  },
  "overrides": {
    "brace-expansion": "^5.0.9",
    "test-exclude": "^8.0.0"
  }
}
